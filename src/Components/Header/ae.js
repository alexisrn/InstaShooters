const main = (params) => {
    var num = 0;
    for(var i = 0; index <= num; i++){
        if((num % 1 === 0 ) && (num % num === 0)){
            return ( main(i))
        }
    } 
 }
 console.log(main(num));